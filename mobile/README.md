# SPPG Mobile App

Aplikasi mobile monitoring SPPG (Satuan Pelayanan Pemenuhan Gizi) menggunakan **React Native Expo**.

## 🚀 Quick Start

1. **Install Dependencies**:
   ```bash
   npm install
   ```
2. **Start App**:
   ```bash
   npx expo start
   ```

## 📱 Features

- **List SPPG**: Halaman daftar SPPG dengan filter status dan progress bar (Lihat di `app/(tabs)/sppg.tsx`).
- **Tech Stack**:
  - Expo SDK 52
  - NativeWind (TailwindCSS)
  - Fonts: Plus Jakarta Sans
  - Icons: Lucide React Native

## 📂 Structure

- `app/(tabs)/sppg.tsx`: Implementasi UI "Daftar SPPG" (Sesuai Prompt).
- `components/`: Komponen reusable.
- `assets/`: Gambar dan font.
